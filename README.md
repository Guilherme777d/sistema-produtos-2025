# Sistema de Produtos

## 📌 Descrição do Projeto
Aplicação web desenvolvida em Java utilizando Spring Boot e Thymeleaf, com o objetivo de gerenciar produtos.  
O sistema permite cadastrar, listar, editar, excluir e ordenar produtos, além de disponibilizar uma API REST para acesso aos dados.

## 🛠️ Tecnologias Utilizadas
- Java 17
- Spring Boot
- Spring MVC
- Spring Data JPA
- Thymeleaf
- MySQL
- Maven

## 📋 Funcionalidades
### Área Web
- Cadastro de produtos
- Listagem de produtos
- Edição de produtos
- Exclusão de produtos
- Ordenação por ID, nome e preço

### API REST
- Listar produtos (`GET /api/produtos`)
- Buscar produto por ID (`GET /api/produtos/{id}`)
- Criar produto (`POST /api/produtos`)
- Atualizar produto (`PUT /api/produtos/{id}`)
- Excluir produto (`DELETE /api/produtos/{id}`)

## 🗂️ Estrutura do Projeto
- `controller` → Controllers Web e REST
- `entity` → Entidades JPA
- `repository` → Repositórios JPA
- `templates` → Páginas HTML (Thymeleaf)

## ▶️ Como Executar o Projeto
1. Clonar o repositório
2. Configurar o banco de dados no `application.properties`
3. Executar a aplicação pela classe `SistemaProdutosApplication`
4. Acessar:
    - Web: `http://localhost:8080/produtos`
    - API: `http://localhost:8080/api/produtos`

# Sistema de Produtos

## 📌 Descrição do Projeto
Aplicação web desenvolvida em Java utilizando Spring Boot e Thymeleaf, com o objetivo de gerenciar produtos.  
O sistema permite cadastrar, listar, editar, excluir e ordenar produtos, além de disponibilizar uma API REST para acesso aos dados.

## 🛠️ Tecnologias Utilizadas
- Java 17
- Spring Boot
- Spring MVC
- Spring Data JPA
- Thymeleaf
- MySQL
- Maven

## 📋 Funcionalidades
### Área Web
- Cadastro de produtos
- Listagem de produtos
- Edição de produtos
- Exclusão de produtos
- Ordenação por ID, nome e preço

### API REST
- Listar produtos (`GET /api/produtos`)
- Buscar produto por ID (`GET /api/produtos/{id}`)
- Criar produto (`POST /api/produtos`)
- Atualizar produto (`PUT /api/produtos/{id}`)
- Excluir produto (`DELETE /api/produtos/{id}`)

## 🗂️ Estrutura do Projeto
- `controller` → Controllers Web e REST
- `entity` → Entidades JPA
- `repository` → Repositórios JPA
- `templates` → Páginas HTML (Thymeleaf)

## ▶️ Como Executar o Projeto
1. Clonar o repositório
2. Configurar o banco de dados no `application.properties`
3. Executar a aplicação pela classe `SistemaProdutosApplication`
4. Acessar:
    - Web: `http://localhost:8080/produtos`
    - API: `http://localhost:8080/api/produtos`

## 👤 Autor
- **Nome:** Guilherme Ferreira Neves
- **RA:** 923107281
- **Curso:** Sistemas de Informação  

